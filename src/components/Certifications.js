export default function Certifications() {
    return (React.createElement("main", null,
        React.createElement("section", { className: "title" },
            React.createElement("h1", null, "No certifications yet - but stay tuned!"))));
}
;
